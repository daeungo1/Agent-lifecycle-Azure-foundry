# Optional legacy Inspector helper only. Do not install into the runtime venv.
# agent-dev-cli requires agent-framework-core<1.3.0, which conflicts with
# Hosted Agent runtime agent-framework-core>=1.10.0 from foundry-hosting.
# Future VS Code tasking should run main.py directly instead of using agentdev.
agent-dev-cli>=0.0.1b260427